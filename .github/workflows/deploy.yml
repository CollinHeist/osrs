# Simple workflow for deploying static content to GitHub Pages
name: Deploy static content to Pages

on:
  # Runs on pushes targeting the default branch
  push:
    branches: ['main']

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# Sets the GITHUB_TOKEN permissions to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow one concurrent deployment
concurrency:
  group: 'pages'
  cancel-in-progress: true

jobs:
  # Single deploy job since we're just deploying
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Node
        uses: actions/setup-node@v6
        with:
          node-version: lts/*
          cache: 'npm'
          cache-dependency-path: |
            tangleroot/package-lock.json
            calculators/package-lock.json

      - name: Install tangleroot dependencies
        working-directory: tangleroot
        run: npm ci

      - name: Build tangleroot
        working-directory: tangleroot
        run: npm run build

      - name: Install calculators dependencies
        working-directory: calculators
        run: npm ci

      - name: Build calculators
        working-directory: calculators
        run: npm run build

      - name: Stage Pages artifact
        run: |
          mkdir -p _site/herblore _site/prayer _site/gear _site/tangleroot _site/training _site/calculators
          cp index.html _site/index.html
          cp herblore/index.html _site/herblore/index.html
          cp prayer/index.html _site/prayer/index.html
          cp gear/index.html _site/gear/index.html
          cp training/index.html _site/training/index.html
          cp -R tangleroot/dist/. _site/tangleroot/
          cp -R calculators/dist/. _site/calculators/

      - name: Setup Pages
        uses: actions/configure-pages@v6

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v4
        with:
          # Upload the staged Pages site
          path: './_site'
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
